{
  "name": "blog-indexeddb-safari-leaks-demo",
  "license": "MIT",
  "source": "src/index.html",
  "scripts": {
    "start": "rimraf dist && parcel --no-source-maps",
    "build": "rimraf dist && parcel build --public-url ./ --no-source-maps",
    "lint": "eslint --ext .js,.html --ignore-path .gitignore --max-warnings 0 .",
    "lint:fix": "yarn lint --fix"
  },
  "devDependencies": {
    "@types/mustache": "^4.1.2",
    "eslint": "^8.5.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-html": "^6.2.0",
    "eslint-plugin-prettier": "^4.0.0",
    "parcel": "^2.0.1",
    "prettier": "^2.5.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "mustache": "^4.2.0",
    "tippy.js": "^6.3.7"
  }
}
